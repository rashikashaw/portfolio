import { Meta } from '@storybook/react';
import { Tabs } from '@portfolio/tabs';
const meta: Meta = {
  title: 'Tabs',
  component: Tabs,
};

export default meta;

export { Default } from './Tabs.default';
