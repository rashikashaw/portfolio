import { Meta } from '@storybook/react';
import { TextOverFlow } from '@portfolio/text-overflow';
const meta: Meta = {
	title: 'Text Over-flow',
	component: TextOverFlow
};

export default meta;

export { Default } from './TextOverFlow.default';
