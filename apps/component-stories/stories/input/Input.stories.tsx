import { Meta } from '@storybook/react';
import { Input } from 'input';
const meta: Meta = {
  title: 'Input',
  component: Input,
};

export default meta;

export { Default } from './Input.default';
