import { Meta } from '@storybook/react';
import { ProgressBar } from 'progress-bar';
const meta: Meta = {
  title: 'ProgressBar',
  component: ProgressBar,
};

export default meta;

export { Default } from './ProgressBar.default';