import { Meta } from '@storybook/react';
import { Tags } from '@portfolio/tags';
const meta: Meta = {
	title: 'Tags',
	component: Tags
};

export default meta;

export { Default } from './Tags.default';