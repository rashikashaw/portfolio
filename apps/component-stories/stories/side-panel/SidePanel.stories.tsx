import { Meta } from '@storybook/react';
import { SidePanel } from '@portfolio/side-panel';
const meta: Meta = {
  title: 'SidePanel',
  component: SidePanel,
};

export default meta;

export { Default } from './SidePanel.default';
